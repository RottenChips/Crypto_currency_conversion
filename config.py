"""
Конфігурація Telegram-бота курсів валют та криптовалют.

Перед запуском встановіть свій BOT_TOKEN:
  1. Отримайте токен у @BotFather
  2. Вставте його нижче або задайте змінну середовища BOT_TOKEN
"""

import os
from dotenv import load_dotenv

# ── Telegram ────────────────────────────────────────────────────────────────
load_dotenv()
BOT_TOKEN: str = os.getenv("BOT_TOKEN", "")  # Вставте свій токен або задайте змінну середовища

# ── CoinGecko API ───────────────────────────────────────────────────────────
COINGECKO_BASE_URL = "https://api.coingecko.com/api/v3"
COINGECKO_TIMEOUT = 10  # секунд

# Криптовалюти, які відстежуються (CoinGecko ID → короткий символ)
CRYPTO_IDS: dict[str, str] = {
    "bitcoin":  "BTC",
    "ethereum": "ETH",
    "solana":   "SOL",
}

# Фіатні валюти для відображення курсів
FIAT_CURRENCIES: list[str] = ["usd", "eur", "uah"]

# Валюти, доступні для конвертації (символ → повна назва)
AVAILABLE_CURRENCIES: dict[str, str] = {
    "USD": "Долар США",
    "EUR": "Євро",
    "UAH": "Гривня",
    "BTC": "Біткоїн",
    "ETH": "Ефіріум",
    "SOL": "Солана",
}

# ── Кешування ───────────────────────────────────────────────────────────────
CACHE_TTL = 60  # секунд (щоб не перевищувати ліміт API)

# ── Повідомлення ────────────────────────────────────────────────────────────
WELCOME_TEXT = (
    "👋 Вітаю в боті курсів валют та криптовалют!\n\n"
    "📊 Курси валют — актуальні курси BTC, ETH, SOL\n"
    "🔄 Конвертація — конвертуйте суму між валютами\n"
    "ℹ️ Допомога — інструкція з використання\n\n"
    "Оберіть дію на клавіатурі нижче 👇"
)

HELP_TEXT = (
    "📖 Як користуватися ботом\n\n"
    "📊 Курси валют\n"
    "Натисніть кнопку — бот покаже актуальні курси BTC, ETH, SOL "
    "до USD, EUR, UAH.\n\n"
    "🔄 Конвертація\n"
    "1. Натисніть «Конвертація»\n"
    "2. Оберіть валюту-джерело (З якої)\n"
    "3. Введіть суму (наприклад: 100)\n"
    "4. Оберіть цільову валюту (В яку)\n"
    "5. Отримайте результат!\n\n"
    "⚠️ Дані оновлюються кожну хвилину (кешування API).\n"
    "Джерело: CoinGecko (безкоштовне API)."
)

ADMIN_ID: int = int(os.getenv('ADMIN_ID', '0'))
